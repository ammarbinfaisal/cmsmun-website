const columnify = {
	fontFamily: "Ubuntu, 'Open Sans', 'Comic Sans', sans-serif",
	display: "flex",
	flexDirection: "column",
	justifyContent: "center",
	alignItems: "center",
	height: "100vh",
	padding: "8vh 5vw",
	marginTop: "-20vh"
};

const heading = {
	fontSize: "calc(1.4em + .5vw + .8vh)", margin: "3vh 0"
}

const para = {
	fontSize: "calc(.8em + .3vw + .6vh)"
}

export default {columnify, heading, para};
